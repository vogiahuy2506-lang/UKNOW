{
  "name": "uknow-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root scaffold cho git hooks (husky). Không build/deploy gì ở đây — backend & frontend tự quản lý.",
  "scripts": {
    "prepare": "husky",
    "test:backend:unit": "cd backend && npm run test:unit",
    "test:backend:integration": "cd backend && npm run test:integration"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
