server {
  listen 80;
  server_name campaign.digiso.vn www.campaign.digiso.vn;

  # Ép toàn bộ truy cập HTTP sang HTTPS khi chạy trên VPS.
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl;
  server_name campaign.digiso.vn www.campaign.digiso.vn;

  # Chứng chỉ được mount từ host VPS vào container qua docker-compose.
  ssl_certificate /etc/letsencrypt/live/campaign.digiso.vn/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/campaign.digiso.vn/privkey.pem;
  ssl_protocols TLSv1.2 TLSv1.3;
  ssl_prefer_server_ciphers on;

  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback: mọi route frontend trả về index.html để React Router xử lý
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Reverse proxy API backend để frontend gọi cùng domain
  location /api/ {
    proxy_pass https://api-campaign.digiso.vn/api/;
    proxy_ssl_server_name on;
    proxy_http_version 1.1;
    proxy_set_header Host api-campaign.digiso.vn;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location /track/ {
    proxy_pass https://api-campaign.digiso.vn/track/;
    proxy_ssl_server_name on;
    proxy_http_version 1.1;
    proxy_set_header Host api-campaign.digiso.vn;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location /file/ {
    proxy_pass https://api-campaign.digiso.vn/file/;
    proxy_ssl_server_name on;
    proxy_http_version 1.1;
    proxy_set_header Host api-campaign.digiso.vn;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location /download/ {
    proxy_pass https://api-campaign.digiso.vn/download/;
    proxy_ssl_server_name on;
    proxy_http_version 1.1;
    proxy_set_header Host api-campaign.digiso.vn;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}
